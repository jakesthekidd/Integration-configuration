{
  "buildCommand": "npm install --include=dev && npx ng build --configuration production",
  "outputDirectory": "dist/field-mapping-app-v2/browser",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
